// Inclue the React library
/*import React from "react";

// Include the react-router module
// Include the Route component for displaying individual routes
// Include the Router component to contain all our Routes
// Include the hashHistory prop to handle routing client side without a server
// Include the IndexRoute (catch-all route)
import {Router, Route, hashHistory, IndexRoute} from "react-router";

// Reference the high-level components
import {Main} from "../../client/components/Main";
import {Home} from "../../client/components/children/Home";
import {Search} from "../../client/components/children/Search";
import {Results} from "../../client/components/children/Results";
import {Saved} from "../../client/components/children/Saved"; 

// Export the Routes
module.exports = (
    <Router history={hashHistory}>
        <Route path="/" component={Main}>
            <Route path="home" component={Home}></Route>
            <Route path="search" component={Search}></Route>
            <Route path="results" component={Results}></Route>
            <Route path="saved" component={Saved}></Route>
        </Route>
    </Router>
); 
*/


